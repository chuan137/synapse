# Curated Test Cases

This directory contains hand-selected trajectory cases for regression testing.

**How to add a case:** `synapse eval-select <task_id>` — copies from `.synapse/cases/` (raw corpus) to here.
**How to remove a case:** `synapse eval-select <task_id> --remove`
**How to list:** `synapse eval-select --list`
